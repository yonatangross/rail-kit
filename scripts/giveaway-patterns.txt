# rail-kit giveaway lint: generic patterns that must never appear in shipped files.
# One extended regex per line; lines starting with # are comments.
# Real names live in an out-of-repo denylist (GIVEAWAY_DENYLIST_FILE), never here.
hq-ext
Yonatan-HQ/
mcp__hq-
hq-ext:collector
clients/_template/[a-z]+-[a-z]+/
project_[a-z_]+_prospect
₪[[:space:]]*[0-9]
[0-9]+[[:space:]]*ש"ח
\$[0-9]{2,}
core#[0-9]+
platform#[0-9]+
